
place_names = ('ones','tens','hundreds','thousands','ten thousands','lakhs','Ten lakhs','Crores','Ten crores','Hundred crores','Thousand crores')
place_values = (1,10,100,1000,10000,100000,1000000,10000000,100000000,1000000000,10000000000)


list_of_apartments = []

print('*****Apartment_list*****\n\n*Press enter if the enumeration done!')
while True:
    current_apt = {}
    current_apt['name'] = input('Apartment name: ')
    if current_apt["name"] == '':
        break

    current_apt['height'] = int(input('Apartment height: '))

    list_of_apartments.append(current_apt)
    print(list_of_apartments[-1])


list_request = input('Do you want final list?(yes or no): ') #.strip().lower()
if list_request is 'yes' or 'Yes' or 'YES':
    print('*****Apartment_list*****\n',list_of_apartments,'\n')


print('Enter 1 to ascending\nEnter 2 for decending\nIf No press Enter: ')
sort_decision = input()

copy_of_original_before_swaping = list_of_apartments.copy()

if sort_decision == '1':
    j = -1
    i = -1
    for apartment in list_of_apartments:
        j += 1
        i += 1
        if j > len(list_of_apartments)-1:
            if list_of_apartments[i]['height'] > list_of_apartments[i+1]['height']:
                continue
            elif list_of_apartments[i]['height'] <  list_of_apartments[i+1]['height']:
                list_of_apartments[i+1], list_of_apartments[i] =  list_of_apartments[i] ,  list_of_apartments[i+1]
            elif  list_of_apartments[i]['height'] ==  list_of_apartments[i]['height']:
                continue
        else:
            break
    print('***Ascending Order***\n',list_of_apartments)

elif sort_decision == '2':
    print('***Descending Order***\n',reverced(list_of_apartments))


elif sort_decision == '':
    print('Have a good day!')

''' totally stucked! Aimode says' its is the name of bubule sort, as the name reflects each bubble is a each value from thire place to the end 
    like a bubble in the soda, for each loop we need to loop again and again until they are no need for swap.
    we use length of the result as a refeerence to use another loop to
    do that, additionally, we have insertion algorithm,
    which matches what i am tried, but whithout clarity '''

